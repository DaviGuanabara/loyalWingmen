from setuptools import setup

setup(
    name="loyal_wingmen",
    version="0.0.1",
    #install_requires=['opencv-python', 'numpy', 'pybullet', 'pytest', 'pygame', "gym"],
    install_requires=[]
)

#Set-ExecutionPolicy -ExecutionPolicy  Unrestricted -Scope Process